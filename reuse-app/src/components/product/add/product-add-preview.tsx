import { ProductAddProps } from '@/types/product';
import React from 'react';

export default function ProductAddPreview({ formData, onNext, onPrev }: Partial<ProductAddProps>) {
  return (
    <div>product-add-preview</div>
  );
}
